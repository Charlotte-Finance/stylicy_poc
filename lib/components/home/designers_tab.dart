import 'package:flutter/material.dart';

class DesignersTab extends StatelessWidget {
  const DesignersTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
      return Container(
        color: Colors.red,
        child: const Text("HOME Tab"),
    );
  }
}
